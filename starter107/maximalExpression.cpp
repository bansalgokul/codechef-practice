#include <iostream>
using namespace std;

void solve() {
    int n, k;
    cin >> n >> k;

    int x1 = n / 2;
    int x2 = (n + k) / 2;
    int x3 = k * (x / k) +

             return;
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}